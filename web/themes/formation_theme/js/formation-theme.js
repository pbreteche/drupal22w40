/**
 * @file
 * Formation behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.formationTheme = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
