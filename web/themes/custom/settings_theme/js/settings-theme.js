/**
 * @file
 * Settings Theme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.settingsTheme = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
